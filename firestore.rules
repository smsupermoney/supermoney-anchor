rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows your server code to query the users collection
    // to find a user by email address during login.
    match /users/{document=**} {
      allow read: if true;
      allow write: if false; // Keep writes restricted for now
    }
  }
}
